import { Link } from "expo-router"
import { View, Text } from "react-native"

export default function BottomOther() {
	return (
		<View className="flex-1 items-center gap-2">
			<Text className="text-2xl">Bottom other screen</Text>
		</View>
	)
}
